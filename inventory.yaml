# the following are ansible keywords: hosts, children, ansible_connection,
# ansible_host, ansible_user, http_port (e.g., 443)

all:
  hosts:
    my_hosts:                    # must match in the playbook
      ansible_connection: local
      ansible_host: localhost    # Replace with your target server IP
      # ansible_user: ubuntu     # Replace with your SSH user